* allows
  * formatting `LogRecord`
* available 
  * built-in
    * `SimpleFormatter`
      * `LogRecord` -- are formatted to -- .txt
    * `XMLFormatter`
      * `LogRecord` -- are formatted to -- XML
  * TP