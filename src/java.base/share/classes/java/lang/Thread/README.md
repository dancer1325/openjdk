* thread
  * := thread of execution | program
  * allowed >= 1 thread / running concurrently | JVM
  * how to create it?
    * `Thread(...)`
      * == constructor
    * `Thread.Builder....build()`
      * == builder