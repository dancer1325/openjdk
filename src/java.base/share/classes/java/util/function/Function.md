* := function / 
  * accepts 1 argument
  * produces 1 result
  * `R apply(T t);`
    * functional method == method / this interface is functional
    * `t` == function argument
    * `R` == function result
* TODO: 